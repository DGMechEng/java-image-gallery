<!DOCTYPE html>
<html>
  <head>
    <title>Update profile for user: {{ user.username }}</title>
    <meta charset="utf-8" />
  </head>
  <body>
<h1>Update profile for user:  {{ user.username }}</h1>
<ul>
  <form action="admin/userUpdated/{{user.username}}" method="GET">
    Password: <input type="text" name="password" value={{user.password}}><br />
    Full Name: <input type="text" name="full_name" value={{user.fullName}}><br />
    <input type="submit" value="Update user" />
  </form>
</ul>
  </body>
</html>
